# Campus Helpdesk — Backend

## Run

From this directory, run:

```bash
npm start
```

The API is available at `http://localhost:3000`. Open Swagger UI at `http://localhost:3000/api-docs` or download the OpenAPI document from `http://localhost:3000/api-docs.json`.
